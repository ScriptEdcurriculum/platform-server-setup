{
	//port on which to run router control application
	"port": 80,
	// routes to various environments
	// name value pairs are subdomain name to port where http traffic should be directed
	"routes": {
		//dev.platform.scripted.org to port 3000
		"dev": 3000,
		//test.platform.scripted.org to port 4000
		"test": 4000,
		//(prod.)platform.scripted.org to port 5000
		"prod": {
			"port": 5000,
			"default": true
		}
	},
	"unknown_default": false
}

